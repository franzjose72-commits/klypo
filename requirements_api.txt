# Dependencias SOLO para api.py en Render.com
# NO incluye paquetes de IA (torch, mediapipe, moviepy, etc.) — esos son para RunPod.
# Build mucho más rápido: ~30s vs ~10min del requirements.txt completo.

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
requests>=2.28.0
python-dotenv>=1.0.0
boto3>=1.34.0
certifi>=2024.0.0
